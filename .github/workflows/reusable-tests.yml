---
name: "Reusable Tests"

on:
    workflow_call:
        inputs:
            run-coverage:
                description: "Run coverage"
                required: false
                type: boolean
                default: false

jobs:
    test-python-code:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: true
            matrix:
                python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
                pytest-version: [""]
                typer-spec: [""]
                include:
                    # Compat checks: ensure plugin works on supported older pytest
                    - python-version: "3.10"
                      pytest-version: "7"
                      typer-spec: ""
                    - python-version: "3.10"
                      pytest-version: "8"
                      typer-spec: ""
                    - python-version: "3.13"
                      pytest-version: "7"
                      typer-spec: ""
                    - python-version: "3.13"
                      pytest-version: "8"
                      typer-spec: ""
                    # Compat checks: typer before and after it vendored click in 0.26
                    - python-version: "3.13"
                      pytest-version: ""
                      typer-spec: "typer>=0.25,<0.26"
                    - python-version: "3.13"
                      pytest-version: ""
                      typer-spec: "typer>=0.26"
        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - name: Setup Python, uv, and the package
              uses: ./.github/actions/setup_python_env
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Pin pytest to ${{ matrix.pytest-version }}.x
              if: matrix.pytest-version != ''
              shell: bash
              run: |
                  floor="${{ matrix.pytest-version }}"
                  ceil=$((floor + 1))
                  uv pip install --python .venv/bin/python "pytest>=${floor},<${ceil}"

            - name: Pin typer to ${{ matrix.typer-spec }}
              if: matrix.typer-spec != ''
              shell: bash
              run: uv pip install --python .venv/bin/python "${{ matrix.typer-spec }}"

            - name: Run tests
              shell: bash
              env:
                  UV_NO_SYNC: "1"
              run: |
                  uv run pytest tests/ src/ --config-file pyproject.toml --color=yes --cov --cov-config=pyproject.toml --cov-report=xml --cov-report=term

            - name: Upload coverage to Codecov
              if: ${{ inputs.run-coverage && matrix.python-version == '3.13' && matrix.pytest-version == '' && matrix.typer-spec == '' }}
              uses: codecov/codecov-action@v7
              with:
                  files: .cache/coverage.xml
              env:
                  CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

    lint-project:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v7

            - uses: actions/cache@v6
              with:
                  path: ~/.cache/prek
                  key: prek-${{ hashFiles('.pre-commit-config.yaml') }}

            - name: Setup Python, uv, and the package
              uses: ./.github/actions/setup_python_env

            - name: run all linters
              shell: bash
              run: uv run duty lint
